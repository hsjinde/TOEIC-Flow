# 專案：TOEIC 每日練習 Web App

## 目標

做一個手機優先的網站，讓我每天花 15 分鐘練多益。資料來源是我自己的 Obsidian 筆記
`D:\my-note\個人學習\多益`。

設計原則：**「每天回來練」優先於「功能很多」**。任何功能如果不能提升回訪率或學習效率，
就往後排。

## 技術棧（已決定）

- **前端**：Next.js (App Router) + TypeScript + Tailwind CSS + shadcn/ui
- **部署**：Cloudflare Pages（用 `@opennextjs/cloudflare`）
- **資料庫**：Cloudflare D1（存使用者進度、SRS 排程、錯題本、作答紀錄）
- **登入**：簡易即可，單人或少數人使用。優先考慮 Cloudflare Access（Zero Trust，
  免寫 auth code）；若不可行則用 Auth.js + Google OAuth。**不要**自己實作密碼儲存。
- **題庫**：build 時產生的靜態 JSON，隨前端打包，不進 D1。題庫是唯讀資料，
  進度才是動態資料——兩者分開。
- **PWA**：可離線練習；離線時作答暫存本機，回線後同步到 D1。

---

## 資料管線

### 來源筆記結構

```
D:\my-note\個人學習\多益\
├── 文法/{6 大類}/{23 章}.md
│     每章含三個可解析區塊：
│       ## 📚 補充秒殺公式      → 5 條速記技巧
│       ## 🔤 相關單字和片語    → 約 13 個，格式 `**word** 詞性 中文 | 例句`
│       ## 💪 練習題（5 題）    → 四選一，選項在同一行 (A)...(B)...(C)...(D)
├── 詳解/{同結構}.md           → 每題的答案、逐選項解析、相關文法點、相似題提醒
├── 閱讀理解/{01 單句填空,02 段落填空,03 篇章閱讀}/*.md
├── 詳解/閱讀理解/{同結構}/*.md
├── 模擬考試/*.md
└── 詳解/模擬考試/*.md
```

### 需求

1. 寫一支 build script，把 markdown 解析成結構化 JSON：
   `questions` / `vocab` / `formulas` / `passages` / `mock_exams`。輸出進版本控制。
2. **Parser 必須容錯且吵鬧**：遇到格式不符的題目要在 build report 列出，
   絕對不可靜默略過。已知的異常案例：
   - `01_名詞與代名詞.md` 第 5 題是「雙空格題」，一題有兩組選項
   - 段落填空題有「句子插入題」，選項是整句而非單字
   - 部分章節選項數可能不是 4
3. 題目與詳解靠「章節路徑 + 題號」對應。build 時驗證每題都有詳解，缺的要讓 build 失敗。
4. 筆記更新後重跑 script 即可更新題庫。**runtime 絕不讀 markdown。**
5. 保留 wikilink 對應關係，讓 App 內可以從題目跳到原始文法章節。

### 題庫成長：AI 批次生成

現有題庫只有 115 題 + 約 300 單字，兩週就刷完，必須能擴充。

- 寫一支獨立的 CLI script（不是 web 功能），用 Claude API 依「章節文法點 + 該章單字表 +
  既有題目風格」生成新題與詳解。
- 一次可指定章節與題數，例如 `pnpm gen --chapter 03/01_不定詞 --count 20`。
- 輸出格式**與現有筆記完全一致的 markdown**，寫進待審資料夾
  （例如 `文法/{類別}/_generated/{章節}.md` 與對應詳解），讓我在 Obsidian 裡審核，
  審過再手動合併進正式筆記。**Obsidian 筆記維持唯一真相來源。**
- 生成的題目要標記 `source: ai`，App 內可篩選，也方便日後回收品質不好的題。
- 生成時要求：貼近多益商用情境（人事/財務/會議/物流/行銷）、四選一、
  誘答選項要是真的常見錯誤而非亂湊。

另外做**程式化生成**（不需要 AI，零成本、可無限出題）：
- 用筆記裡的字尾表（-tion/-ment/-ness/-ity/-ance…）自動生成「選出正確詞性」的 Part 5 題
- 單字的例句挖空 → 例句填空題

---

## 功能範圍

### MVP（第一階段，先做到能每天用）

- **每日任務**：固定配方 10 單字 + 5 文法題 + 1 篇閱讀。完成後打勾、記 streak。
  任務內容由 SRS 排程決定，不是隨機。
- **文法練習**：選章節或隨機，四選一，答完立即顯示筆記裡的完整詳解
  （逐選項分析 + 相關文法點），可一鍵跳到該章節教學內容。
- **單字練習**：英→中、中→英、例句填空三種題型輪替。
- **錯題本**：錯題自動入池，隔天優先出現，連續答對 2 次才畢業。
- **進度統計**：6 大文法類別正確率雷達圖、每日練習量、streak 日曆。
- **登入 + D1 同步**：換裝置進度不遺失。

### 第二階段

- **SRS 排程**：FSRS（或簡化 SM-2），單字與錯題共用同一套排程表。
- **計時模式**：Part 5 每題 20 秒倒數，訓練考試節奏。
- **模擬考模式**：完整計時，結束給預估 TOEIC 分數 + 弱點分析。
- **秒殺公式閃卡**：只刷「📚 補充秒殺公式」，30 秒一輪，通勤用。
- **語音朗讀**：Web Speech API 唸單字與例句（免費、免音檔）。
- **作答時間分析**：找出「會但太慢」的題型。

---

## D1 Schema 草案（可調整）

```sql
users            (id, email, created_at)
daily_progress   (user_id, date, tasks_done, streak_count)
attempts         (id, user_id, item_id, item_type, correct, elapsed_ms, answered_at)
srs_cards        (user_id, item_id, item_type, due_at, interval, ease, reps, lapses)
wrong_pool       (user_id, item_id, item_type, consecutive_correct, added_at)
```

`item_id` 對應題庫 JSON 裡的穩定 ID。**題庫重建時 ID 必須保持穩定**
（用「章節路徑 + 題號」hash，不要用陣列索引），否則使用者進度會錯位。

---

## UX 要求

- 手機優先 RWD；深色模式
- 鍵盤快捷鍵：`1/2/3/4` 選答、`空白鍵` 下一題、`Enter` 確認
- 答題流程零摩擦：選答即判定，不需要按「送出」
- 首屏 < 2s，切題無感延遲
- 錯題的詳解要能收合，複習時可以先自己想

## 不做

- 聽力（Part 1–4）：目前沒有音檔素材
- 社群、排行榜、留言、分享
- 自己實作密碼登入

---

## 執行方式

1. 先跟我確認**題庫 JSON schema** 與**頁面結構**，我點頭再開始寫 code。
2. 第一步只做：build script + 題庫 JSON + 一個能跑通的文法練習流程（純本機儲存）。
   確認資料通了、詳解顯示正確，再接 D1 與登入。
3. build script 完成後給我一份 report：解析出幾題、幾個單字、幾條公式、
   哪些檔案格式異常。
4. 每個階段結束前跑一次驗證，不要在沒實測的情況下說「完成」。
```
